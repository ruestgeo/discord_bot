If creating a command that could use a "utils" hub for common functions, place them in here
and import them in that command file. 
(do not add to or modify the utils.js located in the root of the bot directory)