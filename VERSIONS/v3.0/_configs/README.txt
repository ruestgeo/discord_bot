put additional configs in this directory 
(avoid adding to base directory configs.json)

if the config file needs to be checked for correctness then 
create a _startup function that will perform the verification on that file

it may be useful to add a configsName.README to explain the configs